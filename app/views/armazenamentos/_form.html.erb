<%= form_with(model: armazenamento) do |form| %>
  <% if armazenamento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(armazenamento.errors.count, "error") %> prohibited this armazenamento from being saved:</h2>

      <ul>
        <% armazenamento.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
