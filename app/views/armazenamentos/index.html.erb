<p id="notice"><%= notice %></p>
<div class="page-header">
  <h1>Armazenamentos</h1>
</div>

<table class="table table-striped table-responsive">
  <thead>
    <tr>
      <th>Nome</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @armazenamentos.each do |armazenamento| %>
      <tr>
        <td><%= armazenamento.nome %></td>
        <td>
          <%= link_to 'Visualizar', armazenamento, :class => 'btn btn-success btn-xs'%>
          <%= link_to 'Editar', edit_armazenamento_path(armazenamento), :class => 'btn btn-primary btn-xs' %>
          <%= link_to 'Destroy', armazenamento, method: :delete, :class => 'btn btn-danger btn-xs' ,data: { confirm: 'Você tem certeza?' } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Novo Armazenamento', new_armazenamento_path,:class => 'btn btn-primary' %>
<%=link_to 'Home', welcome_index_path, method: :get, :class => 'btn btn-default' %>