<p id="notice"><%= notice %></p>
<div class="page-header">
  <h1>Produtos</h1>
</div>
<table class="table table-striped table-responsive">
  <thead>
    <tr>
      <th>Nome</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @produtos.each do |produto| %>
      <tr>
        <td><%= produto.nome %></td>
        <td>
          <%= link_to  'Visualizar', produto_path(produto), :class => 'btn btn-success btn-xs' %>
          <%= link_to 'Editar', edit_produto_path(produto),method: :get, :class => 'btn btn-primary btn-xs' %>
          <%= link_to 'Apagar', produto, :class => 'btn btn-xs btn-danger',method: :delete, data: { confirm: 'Você tem certeza?' } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<%= link_to 'Novo produto',new_produto_path, method: :get, :class => 'btn btn-primary' %>
<%=link_to 'Home', welcome_index_path, method: :get, :class => 'btn btn-default' %>
